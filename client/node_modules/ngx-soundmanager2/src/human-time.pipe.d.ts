import { PipeTransform } from '@angular/core';
export declare class HumanTimePipe implements PipeTransform {
    transform(value: number): string;
    /**
     * Pads string with zeros if less than 10
     */
    private pad(d);
}
